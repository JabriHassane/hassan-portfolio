{% extends "blogbase.html.twig" %}

{% block title %}Modifier | {{ work.title }}{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col s12 m12 l12">
                <h4>Modifier l'article {{ work.title }}</h4>
                <img width="300" src="{{ asset('uploads/')~work.picture }}">
            </div>
        </div>
         {% include "includes/work_form.html.twig" %}
    </div>
{% endblock %}